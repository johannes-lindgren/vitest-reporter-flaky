{
  "name": "vite-reporter-flaky-root",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "check:formatting": "prettier . --check",
    "format": "prettier . --write"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
